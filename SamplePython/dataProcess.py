import sklearn.preprocessing as pro

# The following three lines are comments as x_train and x_test aren't defined
# scaler = pro.StandardScaler().fit(x_train[:, 14:])
# x_train[:,14:] = scaler.transform(x_train[:, 14:])
# x_test[:, 14:] = scaler.transform(x_test[:, 14:])
